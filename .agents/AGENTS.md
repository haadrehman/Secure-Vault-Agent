# Project Rules

1. **Continuous Deployment/Commits:** After every prompt/implementation phase where code is modified, the agent MUST run the `./secure_push.sh` script with a descriptive commit message to push changes to GitHub. The user wants to see more commits with clear descriptions.
